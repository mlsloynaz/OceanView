# Shared production build defaults (safe to commit — no secrets).
# Baked into the static bundle at build time; visible in browser DevTools.
VITE_API_BASE_URL=/api
VITE_USE_MOCK_CANDLES=false
VITE_USE_MOCK_MARKET=false
VITE_USE_MOCK_PREMARKET=false
VITE_USE_MOCK_OPERATIONS=false
VITE_USE_MOCK_JOBS_STATUS=false

# Cognito (public SPA client — from oceanview-api stack outputs)
VITE_COGNITO_USER_POOL_ID=us-east-1_kHJ2bHJ6U
VITE_COGNITO_CLIENT_ID=4qvclvmcjnefgqdf892qg5dlak
VITE_AWS_REGION=us-east-1
