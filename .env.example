# OceanView UI — copy to .env.development.local for personal dev settings.
# See docs/environment.md

# Shared defaults (committed): .env.development, .env.production
VITE_API_BASE_URL=/api
VITE_USE_MOCK_CANDLES=false

# Local SAM (gitignored file .env.development.local):
# VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:3001

# Mock mode for offline UI work:
# VITE_USE_MOCK_CANDLES=true
