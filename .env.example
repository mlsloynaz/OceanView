# Default: same-origin /api (CloudFront in prod, Vite proxy in dev)
VITE_API_BASE_URL=/api
VITE_USE_MOCK_CANDLES=false

# Direct API Gateway (bypass CloudFront / CORS) — optional override in .env.local:
# VITE_API_BASE_URL=https://7bko9drijd.execute-api.us-east-1.amazonaws.com/prod
